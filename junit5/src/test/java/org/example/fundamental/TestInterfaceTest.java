package org.example.fundamental;

/*
OBS! Since JUNIT 5 was created based on java 8, we can define tests in interface but we cannot run them there.
The correct approach is to implement the interface in a separate class and run in fact the tests from that class.
 */
class TestInterfaceTest implements JupiterInterfaceTest {

}
